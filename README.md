# IS_test

Banco de preguntas de **Ingeniería del Software (IS)** en formato **JSON**, generado a partir del documento original **`test_IS_todas.docx`**, con estructura compatible con el formato usado en **`SOA_test`**.

---

## 📄 Archivo incluido

- **IS_test.json**  
  Contiene el conjunto completo de preguntas tipo test de la asignatura Ingeniería del Software.

---

## 📊 Contenido del banco

- **Número total de preguntas:** 214  
- **Tipo de preguntas:**  
  - Opción única (`single_choice`)
- **Justificación (`explanation`):**
  - 14 preguntas incluyen justificación.
  - 200 preguntas **no** incluyen justificación (campo vacío).

> ⚠️ Esto refleja fielmente el contenido del documento original: solo algunas preguntas incluían justificación explícita.

---

## ⚙️ Configuración (`settings`)

Todas las opciones están activadas (`true`), tal como se solicitó:

```json
{
  "show_explanation": true,
  "shuffle_questions": true,
  "shuffle_options": true,
  "show_progress": true
}
